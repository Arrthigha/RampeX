#include<stdio.h>
int main()
{
    int arr[]={2,55,99,3,65},max=0;
    for(int i=0;i<=arr[i];i++)
    {
            if(arr[i]>max)
            {
                max=arr[i];
    }
}
    printf("Maximum value : %d ",max);
   return 0;
}
